#include <random>
#include <vector>

using complexd = std::complex<double>;
using complexf = std::complex<float>;

using vecb = std::vector<bool>;
using veci = std::vector<int>;
using vecu = std::vector<unsigned int>;
using vecd = std::vector<double>;
using vecf = std::vector<float>;


using vecci = std::vector<std::complex<int>>;
using veccu = std::vector<std::complex<unsigned int>>;
using veccd = std::vector<std::complex<double>>;
using veccf = std::vector<std::complex<float>>;

using rd = std::random_device;
using distd = std::uniform_real_distribution<double>;
using distf = std::uniform_real_distribution<float>;
using disti = std::uniform_int_distribution<int>;
